
function validar(){
    //recuperar el nombre del formulario
    var nombre = document.getElementById('nombre').value
    
    //validar que este informado
    if (nombre == '') {
        //alert(nombre_error)
        alert($alert_N_O)//Si aparece un error de que esta variable no existe se tiene que incorporar los ficheros en la pagina 2 linea 35
    } 
}
